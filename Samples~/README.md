# Samples

This folder ships the Solana Wallet demo content that Unity imports via the Package Manager. Keep the folder and its `.meta` files together so Unity does not recreate empty directories when installing the package from Git.
